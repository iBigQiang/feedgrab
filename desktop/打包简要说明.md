已完成并推送到 GitHub `feedgrab-desktop` 分支，提交为 `7ee4ffe feat: add Windows desktop packaging`，未合并 `main`。

本地已生成两个 `.exe` 版本：

- 开发者版 portable：`D:\AiCode\feedgrab\desktop\release-packages\20260625-235043\feedgrab Desktop 0.1.0.exe`
- 普通用户安装版：`D:\AiCode\feedgrab\desktop\release-packages\20260625-235043\feedgrab Desktop Setup 0.1.0.exe`

这两个包都已把 Python sidecar worker 和 Playwright Chromium 一起打进 `resources\feedgrab-runtime\`，普通用户首次运行不需要单独下载 Chromium。打包命令现在是：

安装包还会把 `desktop\session-templates\` 内的空白登录态模板复制到安装根目录 `sessions\`。这些模板不包含真实 Cookie；GUI 导入时会忽略空白模板，用户填入真实值或完成登录后才会被识别。

```
cd D:\AiCode\feedgrab\desktop
npm run pack:all
```

每次会输出到 `desktop\release-packages\yyyyMMdd-HHmmss\`，避免 Windows 文件锁导致覆盖失败。说明文档已新增到 [docs/feedgrab-desktop-packaging.md](D:/AiCode/feedgrab/docs/feedgrab-desktop-packaging.md)
