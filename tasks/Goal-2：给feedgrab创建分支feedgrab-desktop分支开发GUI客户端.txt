推荐执行版（中文，可直接复制）

/goal 基于 `D:\AiCode\feedgrab\开发及迭代方案调研报告\feedgrab-GUI客户端长期技术选型综合调研报告.md` 的结论，在 `D:\AiCode\feedgrab` 从 `main` 创建合法 Git 开发分支 `feedgrab-desktop`。只在该新分支内开发 feedgrab 可商业化 GUI 客户端：采用 `Electron + Vite + React + TypeScript + Python Sidecar Worker`，以已完成的 `feedgrab/service/` 第一阶段服务层为基座，先完成 service hardening 和 Python Sidecar Worker 结构化协议，再建立 Electron 商业客户端 MVP，做到普通用户可以完成首次配置、单 URL 和多 URL 抓取、查看实时日志、打开 Markdown 产物、检查登录状态、查看诊断和调整基础设置，同时保持现有 Python CLI、MCP、Markdown 输出、session 和平台抓取行为兼容。

验证：开始前读取并记录 `AGENTS.md`、`CLAUDE.md`、`DEVLOG.md`、`README.md`、`README_en.md`、`pyproject.toml`、`mcp_server.py`、`feedgrab/cli.py`、`feedgrab/reader.py`、`feedgrab/schema.py`、`feedgrab/config.py`、`feedgrab/login.py`、`feedgrab/service/`、`tests/test_service_layer.py`、`tasks/Goal-1：feedgrab 主分支的核心架构升级feedgrab.service服务层.txt` 和 GUI 调研报告；先运行或记录当前基线命令 `git status --short --branch`、`python -m pytest`、`python -m feedgrab.cli`、`python -c "import mcp_server; print('mcp import ok')"`；实现过程中新增并运行 service hardening 测试、Python Sidecar Worker JSONL 或 JSON-RPC 协议测试、日志脱敏测试、任务取消与批量部分失败测试、settings schema 测试、doctor 汇总测试、login 状态查询测试、output artifact 测试；新增 `desktop/` 后先检查仓库实际包管理器约定，若无既有约定则使用 npm 并在 Windows 上优先使用可靠的 `.cmd` 入口，运行 `npm install` 或等价安装、`npm run typecheck`、`npm run lint`、`npm run build`、`npm run test` 或项目最终暴露的最小相关脚本；运行 Python worker smoke，证明 Electron main 能启动 worker、收到 `ready`、提交抓取 job、持续收到 `job_started`、`progress`、`log`、`artifact`、`done` 或结构化 `error` 事件，并能处理 `cancelled`；启动本地 Electron/Vite 开发环境或构建后的桌面应用，截取首屏、抓取页、任务页、输出页、登录页、设置页、诊断页截图，证明 UI 无明显文本重叠和核心入口可用；用无登录或 mock 场景完成一次代表性抓取闭环，返回 Markdown artifact 并能从 GUI 打开文件或目录；最终报告实际分支名、改动文件、运行命令、通过和失败结果、截图或日志证据、未覆盖平台和剩余风险。

约束：不得直接在 `main` 上开发或修改 `main` 历史；不得删除、回滚或覆盖用户已有未提交改动；不得批量删除文件或目录；不得推送远程、合并主分支、创建 PR、发布安装包、部署、写远程服务、上传诊断包、接入真实支付或真实授权服务，除非用户二次确认；不得把 GUI 长期做成 CLI 文本输出解析器；不得让 renderer 直接访问 Node、文件系统、Cookie、API Key、session 原文或 Python 进程；不得把商业授权判断写进 fetcher；不得改变现有 CLI 命令名称、参数语义、默认输出目录、Markdown/front matter、去重索引语义、session 文件格式、MCP `UnifiedContent.to_dict()` 返回形态或平台抓取策略，除非有明确兼容测试和用户确认；不得把平台 Cookie、API Key、storage_state、CDP WebSocket endpoint、URL token、授权 token 或诊断敏感字段写入日志、截图、测试夹具或前端状态；不得一开始做 all-in-one 巨型安装包、真实代码签名、公证、自动更新发布或跨平台 GA 承诺。

边界：允许在新分支内新增 `desktop/` 目录，包含 Electron main、preload、renderer、Vite/React/TypeScript 工程、IPC 类型、桌面端测试、构建配置、资源和文档；允许新增 `feedgrab/worker.py` 或等价 Python Sidecar Worker 入口；允许加固 `feedgrab/service/` 中的 `JobService`、`FetchService`、`SettingsService`、`DoctorService`、`LoginService`、`OutputService`、service models 和 redactor；允许为保持 CLI/MCP 与 service 共用而小步修改 `feedgrab/cli.py`、`mcp_server.py`、`feedgrab/config.py`、`feedgrab/login.py`、`feedgrab/utils/storage.py`、`feedgrab/utils/media.py`、`feedgrab/utils/dedup.py` 中直接相关部分；允许新增或更新 `tests/`、`README.md`、`README_en.md`、`DEVLOG.md` 和必要的 `docs/` 说明；禁止无关重写 `feedgrab/fetchers/`、无关 skills、历史调研报告、支付图片、用户输出目录、`sessions/`、缓存、真实凭据和与 GUI 分支无关的文件。

迭代策略：先列任务清单再写代码，并按项目要求把工作拆给并行子 agent：一个负责 service hardening 和 worker 协议审查，一个负责 Electron/Vite/React 工程骨架和 IPC 安全边界，一个负责 GUI 产品流、登录/诊断/输出库验收和测试矩阵；主线程负责整合、审查、收敛范围和最终验证。第一阶段只做发现和架构确认，复核 GUI 调研报告中“Python 抓取核心不重写、CLI/GUI/MCP 共用 service、UI 不直接 import fetcher、长任务在 Python Sidecar Worker 子进程运行、进度日志产物错误结构化、授权不写入 fetcher、敏感数据默认本地保存”的原则。第二阶段完成 service hardening：`FetchService.fetch_urls()` 对每个 URL 返回成功或失败结构而不是静默跳过，`JobService` 提供 job id、串行默认队列、状态查询、取消、重试、并发限制、历史记录、artifact 汇总和结构化错误，`SettingsService` 提供 typed config schema、默认值、secret 标记、平台能力开关、路径迁移和脱敏预览，`DoctorService` 提供依赖分级、浏览器/CDP/session/输出目录检查和修复建议，`LoginService` 提供平台登录状态、session 年龄、CDP 状态和重新登录入口，`OutputService` 提供 Markdown/CSV/附件 artifact 列表、存在性检查、预览元数据和打开目录能力，redactor 覆盖 dict key、字符串、列表、URL query、Authorization header、Playwright storage_state 和 CDP endpoint。第三阶段实现 Python Sidecar Worker，使用 stdio JSON Lines 或 JSON-RPC over stdio，至少支持 `ping`、`detect_platform`、`fetch`、`cancel`、`doctor`、`settings_snapshot`、`login_status`、`output_list` 方法，至少发出 `ready`、`job_started`、`progress`、`log`、`artifact`、`error`、`done`、`cancelled`、`diagnostic` 事件，worker 只调用 `feedgrab.service`，不直接调用 fetcher，不解析 CLI 文本。第四阶段建立 `desktop/` 工程，Electron main 负责窗口、菜单、系统路径、文件打开、工具发现、worker 生命周期和未来更新通道，preload 只暴露白名单 typed IPC，renderer 使用 React/TypeScript/Vite 构建抓取、任务、输出、登录、设置、诊断、授权占位视图，保持 `nodeIntegration: false`、`contextIsolation: true`、CSP、IPC channel 和 payload 校验。第五阶段完成商业化 MVP 闭环：首次启动向导、输出目录选择、单 URL 抓取、多 URL 抓取、平台识别、实时日志、结果卡片、打开 Markdown、登录中心第一版、诊断页第一版、设置页第一版、license scaffold 和 FeatureGate 占位、update channel 元数据占位。每完成一个聚焦阶段就运行最小相关测试，失败时先读日志和堆栈再改，同一错误连续失败 2 次后必须换证据来源或缩小复现，最多做 3 轮聚焦修正后报告剩余风险并请求用户决策。

完成条件：实际开发分支存在且当前工作区不在 `main`；`feedgrab/service/` 已被加固到可支撑桌面 worker，相关测试覆盖批量部分失败、任务状态、取消、结构化错误、artifact、settings、doctor、login、output 和脱敏；Python Sidecar Worker 能独立启动并通过结构化协议完成至少一个成功 job、一个失败 job 和一个取消 job；`desktop/` Electron/Vite/React/TypeScript 工程可以安装依赖、类型检查、构建并启动；Electron main、preload、renderer 安全边界明确，renderer 无任意文件读写和敏感数据直连能力；GUI 能完成首次配置、抓取任务提交、进度日志展示、结果 artifact 展示和打开目录；CLI smoke、MCP import smoke、Python 测试和前端最小检查通过，或把缺失依赖和不可执行检查用原始错误记录清楚；README、README_en、DEVLOG 或 docs 中记录桌面客户端分支架构、开发命令、验证方式、MVP 限制和下一阶段路线；最终报告列出分支名、主要文件、测试证据、截图或日志、未覆盖平台、跨平台打包和商业化剩余风险。

暂停条件：需要真实平台账号登录、Cookie、API Key、付费 API、生产数据、真实授权服务器、支付服务、代码签名证书、macOS 公证账号、自动更新发布通道、远程推送、合并、发布、部署、批量删除、破坏性清理、修改公开 CLI 行为、修改 Markdown/front matter 或 session 格式、重写大量 fetcher、上传诊断包、收集遥测、法律或商业授权判断、分支命名需要用户拍板、包管理器需要联网但网络受限、下载大型 runtime 或浏览器二进制需要用户授权、或同一阻塞在已有证据下连续 3 次无法推进时暂停并向用户确认。

默认选择理由：用户已经提供了完整 GUI 技术选型报告，且主分支 v0.25.0 已完成第一阶段 `feedgrab/service/`，所以最稳的目标不是重做服务层，也不是做 CLI 按钮面板，而是在独立桌面分支中先稳定 service/worker 协议，再开发 Electron 商业客户端 MVP。

Goal Draft (English-compatible)

/goal Based on `D:\AiCode\feedgrab\开发及迭代方案调研报告\feedgrab-GUI客户端长期技术选型综合调研报告.md`, create a legal Git development branch named `feedgrab-desktop` from `main` or another user-approved clean baseline in `D:\AiCode\feedgrab`; treat the user wording `/feedgrab-desktop` as the desktop branch name rather than a literal leading-slash Git ref, and if the current Codex branch convention requires a prefix, use `codex/feedgrab-desktop` and report the actual branch name. Develop feedgrab's commercializable GUI client only on that new branch, using `Electron + Vite + React + TypeScript + Python Sidecar Worker`, with the completed first-stage `feedgrab/service/` layer as the base. First harden the service layer and define the Python Sidecar Worker structured protocol, then build the Electron commercial desktop MVP so a normal user can complete first-run configuration, run single-URL and multi-URL fetches, see live logs, open Markdown artifacts, inspect login status, view diagnostics, and adjust basic settings while preserving the existing Python CLI, MCP, Markdown output, session files, and platform fetching behavior.

Verification: before implementation, inspect and record `AGENTS.md`, `CLAUDE.md`, `DEVLOG.md`, `README.md`, `README_en.md`, `pyproject.toml`, `mcp_server.py`, `feedgrab/cli.py`, `feedgrab/reader.py`, `feedgrab/schema.py`, `feedgrab/config.py`, `feedgrab/login.py`, `feedgrab/service/`, `tests/test_service_layer.py`, `tasks/Goal-1：feedgrab 主分支的核心架构升级feedgrab.service服务层.txt`, and the GUI research report; run or record the current baseline commands `git status --short --branch`, `python -m pytest`, `python -m feedgrab.cli`, and `python -c "import mcp_server; print('mcp import ok')"`; during implementation add and run service hardening tests, Python Sidecar Worker JSONL or JSON-RPC protocol tests, log redaction tests, cancellation and partial batch failure tests, settings schema tests, doctor summary tests, login status tests, and output artifact tests; after adding `desktop/`, discover the actual package manager convention, default to npm if none exists, prefer reliable `.cmd` entrypoints on Windows, and run install, typecheck, lint, build, and test scripts exposed by the project; run a Python worker smoke proving Electron main can start the worker, receive `ready`, submit a fetch job, receive `job_started`, `progress`, `log`, `artifact`, `done` or structured `error`, and handle `cancelled`; start the local Electron/Vite runtime or built desktop app, capture screenshots for the first screen, fetch page, jobs page, output page, login page, settings page, and diagnostics page, and prove there is no obvious text overlap and the core entry points are usable; complete one representative no-login or mocked fetch workflow from the GUI, return a Markdown artifact, and open the file or directory from the GUI; final reporting must include actual branch name, changed files, commands run, passing and failing results, screenshot or log evidence, uncovered platforms, and remaining risks.

Constraints: do not develop directly on `main` or rewrite `main` history; do not delete, revert, or overwrite existing user changes; do not bulk delete files or directories; do not push remotely, merge into main, create a PR, publish installers, deploy, write remote services, upload diagnostic bundles, connect real payment, or connect a real license service without explicit user confirmation; do not turn the GUI into a long-term CLI text parser; do not let renderer directly access Node, the filesystem, cookies, API keys, raw sessions, or the Python process; do not put commercial license checks inside fetchers; do not change existing CLI command names, argument semantics, default output directory behavior, Markdown or front matter format, dedup index semantics, session file format, MCP `UnifiedContent.to_dict()` shape, or platform fetching strategy unless compatibility tests and user confirmation justify the change; do not log platform cookies, API keys, storage state, CDP WebSocket endpoints, URL tokens, license tokens, or diagnostic secrets in logs, screenshots, fixtures, or frontend state; do not start with an all-in-one giant installer, real code signing, notarization, automatic update release, or cross-platform GA promise.

Boundaries: writes are allowed on the new branch under a new `desktop/` directory for Electron main, preload, renderer, Vite React TypeScript project files, IPC types, desktop tests, build config, assets, and docs; writes are allowed for a new `feedgrab/worker.py` or equivalent Python Sidecar Worker entry point; writes are allowed to harden `feedgrab/service/` including `JobService`, `FetchService`, `SettingsService`, `DoctorService`, `LoginService`, `OutputService`, service models, and redaction helpers; minimal directly related edits are allowed in `feedgrab/cli.py`, `mcp_server.py`, `feedgrab/config.py`, `feedgrab/login.py`, `feedgrab/utils/storage.py`, `feedgrab/utils/media.py`, and `feedgrab/utils/dedup.py` only when required to keep CLI, MCP, and service shared; tests, `README.md`, `README_en.md`, `DEVLOG.md`, and necessary `docs/` updates are allowed; do not rewrite unrelated `feedgrab/fetchers/`, unrelated skills, historical research reports, payment images, user output directories, `sessions/`, caches, real credentials, or files unrelated to the GUI branch.

Iteration policy: list the task checklist before coding and delegate work to parallel sub-agents as required by the project: one for service hardening and worker protocol review, one for the Electron/Vite/React scaffold and IPC security boundary, and one for GUI product flow, login, diagnostics, output library acceptance, and test matrix; the main agent integrates, reviews, narrows scope, and performs final verification. First perform discovery and architecture confirmation against the report principles: do not rewrite the Python fetching core, share service across CLI, GUI, and MCP, do not import fetchers from UI, run long tasks in the Python Sidecar Worker process, structure progress logs artifacts and errors, keep licensing outside fetchers, and save sensitive data locally by default. Next harden the service layer so `FetchService.fetch_urls()` returns per-URL success or failure structures instead of silently skipping failures, `JobService` provides job IDs, a serial default queue, status query, cancellation, retry, concurrency limits, history, artifact summary, and structured errors, `SettingsService` provides typed config schema, defaults, secret flags, platform capability switches, path migration, and redacted previews, `DoctorService` provides dependency tiers, browser CDP session output directory checks and repair suggestions, `LoginService` provides platform login status, session age, CDP status, and relogin entry points, `OutputService` provides Markdown CSV attachment artifact lists, existence checks, preview metadata, and open-directory support, and redaction covers dict keys, strings, lists, URL query, Authorization headers, Playwright storage state, and CDP endpoints. Next implement the Python Sidecar Worker over stdio JSON Lines or JSON-RPC with at least `ping`, `detect_platform`, `fetch`, `cancel`, `doctor`, `settings_snapshot`, `login_status`, and `output_list` methods, and at least `ready`, `job_started`, `progress`, `log`, `artifact`, `error`, `done`, `cancelled`, and `diagnostic` events. Next create `desktop/`, where Electron main owns windows, menus, system paths, file opening, tool discovery, worker lifecycle, and future update-channel metadata, preload exposes only whitelisted typed IPC, and renderer uses React, TypeScript, and Vite for fetch, jobs, output, login, settings, diagnostics, and license placeholder views with `nodeIntegration: false`, `contextIsolation: true`, CSP, and IPC channel and payload validation. Next complete the commercial MVP loop: first-run wizard, output directory selection, single URL fetch, multi URL fetch, platform detection, live logs, result cards, open Markdown, first login center, first diagnostics page, first settings page, license scaffold with FeatureGate placeholder, and update-channel metadata placeholder. After each focused phase run the smallest relevant checks, inspect logs and tracebacks before editing, change evidence source or reduce to a smaller reproduction after two consecutive failures on the same issue, and after three focused repair rounds report remaining risks and ask the user for a decision.

Stop when: the actual development branch exists and the workspace is not on `main`; `feedgrab/service/` is hardened enough for a desktop worker and tests cover partial batch failure, job status, cancellation, structured errors, artifacts, settings, doctor, login, output, and redaction; the Python Sidecar Worker starts independently and completes at least one successful job, one failed job, and one cancelled job through the structured protocol; the `desktop/` Electron/Vite/React/TypeScript project can install dependencies, typecheck, build, and start; Electron main, preload, and renderer security boundaries are explicit and renderer has no arbitrary file access or direct sensitive-data access; the GUI can complete first-run configuration, submit a fetch job, show progress logs, display result artifacts, and open the output directory; CLI smoke, MCP import smoke, Python tests, and frontend checks pass or missing dependencies and unexecutable checks are recorded with raw errors; README, README_en, DEVLOG, or docs document the desktop branch architecture, development commands, verification method, MVP limits, and next-stage roadmap; the final report lists branch name, main files, verification evidence, screenshots or logs, uncovered platforms, packaging gaps, and commercial risks.

Pause if: real platform login, cookies, API keys, paid APIs, production data, a real license server, payment service, code-signing certificates, a macOS notarization account, automatic update release channel, remote push, merge, release, deployment, bulk deletion, destructive cleanup, public CLI behavior changes, Markdown or front matter format changes, session format changes, broad fetcher rewrites, diagnostic upload, telemetry collection, legal or commercial licensing judgment, branch naming decision, package installation blocked by network, large runtime or browser binary download needing user approval, or the same blocker repeats three times under the available evidence.

补充执行约定（适用于中英文 Goal）：当前 `main` 没有 `package.json`、Vite、Electron、React 或 `desktop/` 工程，因此前端命令只能在新分支真实创建 `desktop/package.json` 且脚本存在后运行；Python 侧优先先跑 `python -m pytest tests/test_service_layer.py -q`、`python -c "from feedgrab.service import FetchService; print('service import ok')"`、`python -c "import mcp_server; print('mcp import ok')"`，再跑全量 `python -m pytest tests/ -q` 或 `python -m pytest`；`FetchService.fetch_url()` 会继承保存 Markdown、附件下载和去重索引写入等副作用，自动化测试必须使用 fake reader、临时 `OUTPUT_DIR` 或隔离目录，真实平台抓取只作为用户授权后的手动验收；验证过程中不得运行 `feedgrab reset`、`feedgrab clean-index` 或任何会删除用户输出、缓存、session、索引的命令；最终测试数量以当前实际输出为准，不沿用 DEVLOG、AGENTS 或历史报告里的固定数字。
